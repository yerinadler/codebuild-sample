version: 0.2
phases:
  pre_build:
    commands:
      - echo Logging in to AWS ECR
      - ls -lah
      - docker ps
  build:
    commands:
      - echo $CODEBUILD_SOURCE_VERSION
      - echo Building
  post_build:
    commands:
      - echo Finished build
      - docker ps